<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">


    <uses-permission android:name="android.permission.INTERNET">
    </uses-permission>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".user_interfaces.MainActivity"
            android:exported="true"
            android:label="prestamos"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity
                android:name=".user_interfaces.PrestamosActivity"
                android:label="@string/app_name">
                <!-- Puedes agregar intent-filters si es necesario -->
            </activity>

            <activity
                android:name=".user_interfaces.DispositivosActivity"
                android:label="@string/app_name">
                <!-- Agregar intent-filters si es necesario -->
            </activity>
        <activity android:name=".user_interfaces.PrestamoFormActivity" />




    </application>

</manifest>